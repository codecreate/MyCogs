const proxy = require('http-proxy-middleware');

module.exports = function(app) {
    app.use(proxy('/cogsapi/authorise', { target: 'http://localhost:9000/' }));
    app.use(proxy('/cogsapi/callback', { target: 'http://localhost:9000/' }));
};